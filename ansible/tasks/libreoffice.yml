---
- name: Libreoffice
  apt:
    name: "{{ packages }}"
  vars:
    packages:
    - libreoffice
    - libreoffice-l10n-eu
    - libreoffice-help-eu
    - libreoffice-l10n-es
    - libreoffice-help-es
