---
- hosts: sortu
  connection: local
  vars:
    - nora: "{{ inventory_hostname }}"
    - nondik: /home/scriptak/konfigurazioak

  tasks:
    - include: tasks/ldap.yml ansible_connection=chroot
    - include: tasks/xfce.yml ansible_connection=chroot
    - include: tasks/firefox.yml ansible_connection=chroot
    - include: tasks/evince.yml ansible_connection=chroot
    - include: tasks/smplayer.yml ansible_connection=chroot
    - include: tasks/irten.yml ansible_connection=chroot
    - include: tasks/xfcekonf.yml bat={{ nondik }} bi={{ nora }}
    - include: tasks/firefoxkonf.yml bat={{ nondik }} bi={{ nora }}
    - include: tasks/libreofficekonf.yml bat={{ nondik }} bi={{ nora }}
    - include: tasks/ldapkonf.yml bat={{ nondik }} bi={{ nora }}
    - include: tasks/smplayerkonf.yml bat={{ nondik }} bi={{ nora }}
