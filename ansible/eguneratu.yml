---
- hosts: sortu
  connection: local
  tasks:

   - name: Updating host using apt
     apt:
             update_cache: yes
             upgrade: dist
   - name: Clean unwanted olderstuff
     apt:
             autoremove: yes
             purge: yes
