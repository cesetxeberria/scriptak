#wayland idazmahai minimo bat instalatu. Debian.
---

  - include: 00-labwc.yml
    when: ansible_distribution_release == 'bookworm'

  - include: 01-sfwbar.yml
    when: ansible_distribution_release == 'bookworm'

  - include: 02-proiektorea.yml
    when: ansible_distribution_release == 'bookworm'

  - include: 03-extra.yml
    when: ansible_distribution_release == 'bookworm'
